name: Trigger Worker

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger worker
        run: |
          curl -fsS "${{ secrets.TRIGGER_URL }}"
